[project]
name = "kpxcsudo"
version = "0.1.0"
description = "Using a KeepassXC database to store your sudo passwords."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pykeepass>=4.1.1.post1",
]

[dependency-groups]
dev = [
    "nuitka>=4.1.3",
]
